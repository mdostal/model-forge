# Real measurements from the 2026-09-10 photo drop (14 photos,
# reference-photos/PXL_20260910_*).
#
# CORRECTED 2026-09-10, third pass — the hole/width numbers below come
# directly from the user after two failed attempts at reading the caliper
# photos myself (upside-down LCDs misread twice in a row). Trust these
# verbal numbers over any further photo re-reading:
#   - hole: 12mm long x 7mm wide
#   - margin from hole edge to the part's outer edge: ~4mm
#   => strip width = 7 (hole width) + 4 + 4 (margin each side) = 15mm
# This replaces an earlier wrong reading that took the hole's 12mm LENGTH
# for the strip's WIDTH.
#
# Face lengths (21.7mm / 26mm, unequal) came from a still-earlier
# caliper-photo read and were NOT disputed by the user's correction —
# kept as-is.
#
# Shape: small L-bent brass/bronze clip, ONE oval hole on one face (12x7mm,
# constant-width stadium — no dogbone/keyhole, that was an earlier
# over-read of "4mm/7mm" from the original verbal description), no
# separate hex-bolt hole on the other face (hole_diameter=0 omits it).
#
# Thickness: native material measures ~1.15mm (5 caliper readings,
# 0.93-1.34mm), well under the original ~1.8mm verbal estimate — flagged,
# not re-litigated here. Print target (1.75mm) is the user's own explicit
# choice, independent of native thickness.

face_a_length: 21.7    # measured (caliper): the foot, no hole
face_b_length: 26      # measured (caliper): the arm carrying the oval hole
width: 15               # derived: hole_width(7) + margin(4) + margin(4), per user correction
material_thickness: 1.75  # first-attempt PRINT thickness — user's explicit target
material_source: print-native   # printing at an explicit target, not a derived bump
print_material: PLA

# No hex-bolt hole on this part — omit that feature.
hole_diameter: 0
countersink_diameter: 0
countersink_depth: 0
hole_offset: 0

# The oval hole on face_b — per user correction: 12mm long x 7mm wide,
# a plain constant-width stadium slot (matches the template).
slot_length: 12
slot_width: 7
slot_offset: 16   # estimate: positions the hole ~4mm from the tip of the 26mm
                   # arm (26 - 4 - 6), consistent with the stated 4mm edge
                   # margin — still not a direct caliper reading for THIS
                   # specific distance, flag if it prints off-center
